# admin


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

[`WorkspaceAdmin`](https://answerdotai.github.io/fastgws/admin.html#workspaceadmin)
keeps the Admin Directory and Enterprise License Manager clients
together. User creation, licensing, suspension, and deletion remain
separate operations, so callers choose the lifecycle they need.

## Users

Construct the administrator from credentials that include
`admin.directory.user` and `apps.licensing`. `create_user` mirrors
Google’s required identity fields while keeping the password and
organizational policy explicit.

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/fastgws/blob/main/fastgws/admin.py#L17"
target="_blank" style="float:right; font-size:smaller">source</a>

### WorkspaceAdmin

``` python
def WorkspaceAdmin(
    creds
):
```

*High-level Workspace user and license administration. `creds` need the
`admin.directory.user` and `apps.licensing` scopes.*

## Licences and lifecycle

Licensing is deliberately independent from creation because a domain can
auto-assign a licence by organizational unit. `assign_license` and
`remove_license` use explicit product and SKU IDs; suspension and
deletion only change the Directory user.

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/fastgws/blob/main/fastgws/admin.py#L40"
target="_blank" style="float:right; font-size:smaller">source</a>

### WorkspaceAdmin.assign_license

``` python
async def assign_license(
    user_id:str, # User's primary email address
    sku_id:str, # Workspace product SKU
    product_id:str='Google-Apps', # Workspace product ID
):
```

*Assign a product licence to a user*

`remove_license` revokes one explicit product/SKU assignment without
changing the Directory user.

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/fastgws/blob/main/fastgws/admin.py#L52"
target="_blank" style="float:right; font-size:smaller">source</a>

### WorkspaceAdmin.remove_license

``` python
async def remove_license(
    user_id:str, # User's primary email address
    sku_id:str, # Workspace product SKU
    product_id:str='Google-Apps', # Workspace product ID
):
```

*Remove a product licence from a user*

`suspend_user` can suspend or restore an account independently of its
licences.

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/fastgws/blob/main/fastgws/admin.py#L64"
target="_blank" style="float:right; font-size:smaller">source</a>

### WorkspaceAdmin.suspend_user

``` python
async def suspend_user(
    user_key:str, # Primary email, alias, or immutable user ID
    suspended:bool=True, # Suspend rather than restore the user?
):
```

*Set a user’s suspension state*

`delete_user` permanently removes the Directory user and remains a
separate, explicit operation.

------------------------------------------------------------------------

<a
href="https://github.com/answerdotai/fastgws/blob/main/fastgws/admin.py#L75"
target="_blank" style="float:right; font-size:smaller">source</a>

### WorkspaceAdmin.delete_user

``` python
async def delete_user(
    user_key:str, # Primary email, alias, or immutable user ID
):
```

*Delete a Workspace user*

A complete provisioning sequence stays explicit:

``` python
scopes = ['https://www.googleapis.com/auth/admin.directory.user',
          'https://www.googleapis.com/auth/apps.licensing']
creds = await oauth_creds(account='admin@example.com', scopes=scopes)
admin = WorkspaceAdmin(creds)
user = await admin.create_user('new@example.com', 'New', 'User', password,
                               org_unit_path='/Internal')
await admin.assign_license(user.primaryEmail, 'workspace-sku-id')
```

If the organizational unit auto-licenses new users, inspect the
assignment first and omit the final call.
